/**
 * MVP submitScore script placeholder.
 * Intended to call contract.submitScore(runIdHash, score, attestationHash).
 */

const example = {
  runIdHash: '0x<sha256(runId)>',
  score: 4096,
  attestationHash: '0x<sha256(attestation-token-or-payload)>'
};

console.log('TODO: call submitScore(runIdHash, score, attestationHash)', example);
