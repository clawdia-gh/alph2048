/**
 * MVP startRun script placeholder.
 * Intended to call contract.startRun(runIdHash, seedHash) with 0.1 ALPH fee attached.
 */

const example = {
  runIdHash: '0x<sha256(runId)>',
  seedHash: '0x<sha256(seed)>',
  feeAttoAlph: '100000000000000000' // 0.1 ALPH
};

console.log('TODO: call startRun(runIdHash, seedHash) with 0.1 ALPH', example);
